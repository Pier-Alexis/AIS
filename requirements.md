# **Here is the only required packages used by AIS for now, january first 2025**
### Python
- *scapy*
- *psutils*

***To install them, you need to run `pip install scapy psutils psutil` before launching the application.***
